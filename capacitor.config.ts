import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.zombiesurvivor.game',
  appName: 'Zombie Survivor',
  webDir: 'dist'
};

export default config;
